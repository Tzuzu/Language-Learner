<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Include Bootstrap CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <title>Language Learning</title>
  </head>
  <body>
    <button type="button" class="btn btn-danger" id="start">Start Quiz</button>

    {{#each questions as |question questionId|}}
    <div id="question-{{questionId}}" class="question" style="display: none;">
        {{question.question_text}}
        <br />
        <div>
            {{#each question.answers as |answer answerId|}}
                <div class="form-check">
                    <input
                        class="form-check-input"
                        type="radio"
                        data-is-correct="{{answer.is_correct}}"
                        id="answer-q{{questionId}}-a{{answerId}}"
                        onchange="displayButton()"
                    />
                    <label
                        class="form-check-label"
                        for="answer-q{{questionId}}-a{{answerId}}"
                    >{{answer.answer_text}}</label>
                </div>
            {{/each}}
            <button type="button" class="btn btn-danger" id="submit" style="display: none;">Submit Answer</button>
        </div>
    </div>
    {{/each}}

    <!--This section gives the results with buttons to go back to the home page, review page, or retry the lesson.-->
    <div id='results'>
        <div class="p-5 text-center bg-body-tertiary rounded-3">
    <img src="../assets/images/Web capture_6-7-2023_195640_127.0.0.1.jpeg" alt="logo" width="100" height="100"><use xlink:href="#bootstrap"></use></img>
    <h1 class="text-body-emphasis">CONGRATULATIONS!</h1>
    <p class="col-lg-8 mx-auto fs-5 text-muted">
      Congrats, you've finished the lesson with a final score of
            <span id="finalScore">0</span>%! You may retry this lesson, leave a
            review, or return home to take another lesson!
    </p>
    <div class="d-inline-flex gap-2 mb-5">
      <button class="d-inline-flex align-items-center btn btn-secondary btn-lg px-4 rounded-pill" type="button">
        Return Home
      </button>
      <button class="btn btn-outline-secondary btn-lg px-4 rounded-pill" type="button">
        Retry
      </button>
      <button class="d-inline-flex align-items-center btn btn-secondary btn-lg px-4 rounded-pill" type="button">
        Leave a Review
      </button>
    </div>
  </div>

    <!-- Include Bootstrap JavaScript -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="./js/japanese.js"></script>
  </body>
</html>
