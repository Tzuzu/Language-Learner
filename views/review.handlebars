
    {{#each reviews as |review|}}
    <div class="row mb-2">
        <div class="col-md-8">
            <h4><a href="/reviews/{{review.id}}">{{review.username}}</a></h4>
        </div>
    {{/each}}
    </div>
{{/if}}
</div>